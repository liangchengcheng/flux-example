package mddemo.library.com.readme;

import android.view.View;

/**
 * Created by chengcheng on 2015/11/28.
 */
public class readme {

   // 应用被分成三个主要部分：

  // View: 应用的界面。这里创建响应用户操作的action。

   // Dispatcher: 中心枢纽，传递所有的action，负责把它们运达每个Store。

   // Store: 维护一个特定application domain的状态。它们根据当前状态响应action，执行业务逻辑，

   // 同时在完成的时候发出一个change事件。这个事件用于view更新其界面。
}
